# ══════════════════════════════════════════════════════
# IraqNow Bot — متطلبات Python
# البيئة المستهدفة: Termux على Android (Python 3.11+)
# التشغيل: pip install -r requirements.txt
# ══════════════════════════════════════════════════════

# ── Telegram ─────────────────────────────────────────
pyTelegramBotAPI>=4.14.0

# ── شبكة / HTTP ──────────────────────────────────────
requests>=2.31.0

# ── RSS / تغذيات الأخبار ──────────────────────────────
feedparser>=6.0.10

# ── جدولة المهام ─────────────────────────────────────
APScheduler>=3.10.4

# ── تحليل HTML ───────────────────────────────────────
beautifulsoup4>=4.12.0

# ── الذكاء الاصطناعي (اختياري — اضبط المفاتيح في .env) ──
# google-generativeai (SDK القديم) اختياري
google-generativeai>=0.3.0

# google-genai (SDK الجديد) — قد لا يعمل على Termux بعض الأحيان
# عطِّل هذا السطر إذا واجهت مشاكل في التثبيت على Termux
google-genai>=0.1.0

groq>=0.4.0
openai>=1.10.0
cohere>=4.40
mistralai>=0.1.0
together>=0.2.0

# ── تحويل النص إلى صوت (TTS) ────────────────────────
# edge-tts: مجاني تماماً — يعمل بشكل ممتاز على Termux
edge-tts>=6.1.0
# gTTS: بديل — يحتاج اتصال بـ Google
gTTS>=2.4.0

# ── التوقيت والمناطق الزمنية ─────────────────────────
pytz>=2024.1

# ── مراقبة النظام (اختياري) ─────────────────────────
# psutil: تتبع RAM/CPU — مُعالَج بـ try/except في الكود إذا لم يُثبَّت
psutil>=5.9.0

# ── متغيرات البيئة من ملف .env (اختياري على Termux) ──
# يُمكِّن قراءة .env تلقائياً بدل ضبط export يدوياً
python-dotenv>=1.0.0

# ══════════════════════════════════════════════════════
# ملاحظات Termux:
# 1. ثبِّت Python: pkg install python
# 2. ثبِّت الأدوات الأساسية: pkg install python-pip git
# 3. بعض الحزم تحتاج: pkg install build-essential libffi-dev
# 4. لتثبيت edge-tts: pip install edge-tts
#    (لا تحتاج إلى حزم نظام إضافية)
# 5. google-genai قد تحتاج: pip install --no-build-isolation google-genai
# ══════════════════════════════════════════════════════

# FIX-8: TTL caches
cachetools>=5.3.0
# FIX-10/15: Circuit Breaker (للمرحلة 3)
# pybreaker>=1.2.0
